# QuantX Branching Status

The architecture drafts currently live on the repository baseline while the first architecture branch is being established. Once `docs/architecture-roadmap` is created, future architecture edits should target that branch rather than `main`.
