# QuantX Architecture Freeze Point

After the architecture review passes, the approved specification becomes the reference for implementation. Changes to core boundaries, domain contracts, execution semantics or plugin contracts should require an ADR rather than ad-hoc edits.

Feature work may proceed in parallel only when it stays within the approved boundaries.
